import { styled } from '../../../stitches.config'

export const HeaderSubTitle = styled('div', {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  marginBottom: '1.25rem',
})

export const SectionRatedBooks = styled('section', {
  display: 'flex',
  flexDirection: 'column',
  gap: '0.75rem',
})
